spring:
  application:
    name: Backend-Spring
  datasource:
    url: jdbc:postgresql://localhost:54320/library_bd
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG